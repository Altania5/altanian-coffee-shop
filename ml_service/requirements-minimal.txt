# Minimal requirements for testing (Python 3.8 32-bit compatible)
# These have pre-built wheels available

# Core libraries
numpy>=1.19.0,<1.25.0
pandas>=1.3.0,<2.1.0

# ML libraries (try to get pre-built)
scikit-learn>=1.0.0,<1.4.0
xgboost>=1.5.0,<2.0.0

# Preprocessing
category-encoders>=2.5.0

# Web framework
flask>=2.0.0,<3.0.0
flask-cors>=3.0.0

# Utilities
joblib>=1.0.0
python-dotenv>=0.19.0

# SHAP (optional - skip if build fails)
# shap>=0.40.0
